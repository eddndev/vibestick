---

---

<section
    id="lab-section"
    class="relative w-full min-h-screen flex items-center justify-center pointer-events-auto"
>
    <!-- Container -->
    <div
        class="container mx-auto px-6 md:px-12 grid grid-cols-1 md:grid-cols-2 gap-12 items-center"
    >
        <!-- Left Side: Informative Text (Mirrored Layout) -->
        <div
            class="flex flex-col gap-8 lab-content opacity-0 translate-y-10 order-2 md:order-1"
        >
            <div class="w-16 h-[2px] bg-secondary mb-2 block"></div>

            <h2
                class="text-4xl md:text-5xl font-black uppercase tracking-tight text-white mb-2"
            >
                Sintetizado en<br />
                <span
                    class="text-transparent bg-clip-text bg-gradient-to-r from-white to-secondary"
                    >Laboratorio</span
                >
            </h2>

            <p
                class="text-lg md:text-xl text-muted font-light leading-relaxed max-w-lg"
            >
                No es producción masiva. Cada Eheco-Stick es sintetizado
                individualmente bajo estrictos estándares científicos,
                combinando la precisión de la ingeniería química con la atención
                al detalle de una pieza única.
            </p>

            <div
                class="grid grid-cols-1 md:grid-cols-2 gap-6 text-sm md:text-base mt-4 border-t border-white/5 pt-8"
            >
                <div class="space-y-2">
                    <h3 class="text-white font-bold uppercase tracking-wider">
                        Síntesis Química
                    </h3>
                    <p class="text-muted leading-relaxed">
                        Formulación controlada para máxima actividad.
                    </p>
                </div>
                <div class="space-y-2">
                    <h3
                        class="text-secondary font-bold uppercase tracking-wider"
                    >
                        Cuidado Artesanal
                    </h3>
                    <p class="text-muted leading-relaxed">
                        Supervisión humana experta en cada paso de curado.
                    </p>
                </div>
                <div class="space-y-2">
                    <h3 class="text-white font-bold uppercase tracking-wider">
                        Estándares Altos
                    </h3>
                    <p class="text-muted leading-relaxed">
                        Verificación rigurosa de cada lote. Cero defectos.
                    </p>
                </div>
            </div>
        </div>

        <!-- Right Side: Empty for 3D Model -->
        <div class="hidden md:block order-1 md:order-2"></div>
    </div>
</section>

<script>
    import gsap from "gsap";
    import { ScrollTrigger } from "gsap/ScrollTrigger";

    gsap.registerPlugin(ScrollTrigger);

    const tl = gsap.timeline({
        scrollTrigger: {
            trigger: "#lab-section",
            start: "top 60%",
            end: "bottom 40%",
            toggleActions: "play none none reverse",
        },
    });

    tl.to(".lab-content", {
        y: 0,
        opacity: 1,
        duration: 1,
        ease: "power3.out",
    });
</script>
