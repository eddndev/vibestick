---
const products = [
  { id: 1, name: "Urban Breath", price: "$15.00", color: "bg-blue-500" },
  { id: 2, name: "Nature Call", price: "$12.50", color: "bg-green-500" },
  { id: 3, name: "Cyber Purify", price: "$18.00", color: "bg-purple-500" },
  { id: 4, name: "Solar Clean", price: "$14.00", color: "bg-yellow-500" },
  { id: 5, name: "Night Filter", price: "$16.00", color: "bg-indigo-500" },
  { id: 6, name: "Toxic Eater", price: "$20.00", color: "bg-red-500" },
];
---

<section
  id="products"
  class="min-h-screen py-20 bg-transparent relative pointer-events-none"
>
  <!-- Content hidden for scrollytelling preview
  <div class="container mx-auto px-4">
    <h2 class="text-4xl md:text-5xl font-bold mb-12 text-center">
      Nuestra Colección
    </h2>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
      {products.map((product) => (
        <div class="group relative aspect-square bg-surface rounded-3xl overflow-hidden cursor-pointer">
       
            <div class={`absolute inset-0 ${product.color}/20 flex items-center justify-center transition-transform duration-500 group-hover:scale-110`}>
                <span class="text-6xl opacity-50">✨</span>
            </div>
            
        
            <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6">
                <h3 class="text-2xl font-bold text-white translate-y-4 group-hover:translate-y-0 transition-transform duration-300">{product.name}</h3>
                <p class="text-primary font-medium translate-y-4 group-hover:translate-y-0 transition-transform duration-300 delay-75">{product.price}</p>
            </div>
        </div>
      ))}
    </div>
  </div>
  -->
</section>
