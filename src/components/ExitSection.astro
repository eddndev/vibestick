---

---

<section
    id="exit-section"
    class="relative w-full h-[80vh] md:h-[80vh] flex items-center justify-center pointer-events-none overflow-hidden"
>
    <!-- Background Image -->
    <div class="absolute inset-0 z-0">
        <img
            src="/assets/images/stickers.jpg"
            alt=""
            class="w-full h-full object-cover opacity-30"
        />
    </div>

    <!-- Optional: Subtle text or just empty -->
    <div class="relative z-10 text-center opacity-50">
        <!-- Empty for now -->
    </div>
</section>

<style>
    .mask-gradient-b {
        mask-image: linear-gradient(
            to bottom,
            transparent,
            black 20%,
            black 80%,
            transparent
        );
        -webkit-mask-image: linear-gradient(
            to bottom,
            transparent,
            black 20%,
            black 80%,
            transparent
        );
    }

    @media (max-width: 767px) {
        #exit-section {
            height: 40vh;
        }
    }
</style>
